# Android / Gradle build artifacts
.gradle/
build/
app/build/
*.apk
*.aab
*.ap_
*.dex
*.jks
*.keystore

# Local properties (contains SDK path, machine-specific)
local.properties

# IDE
.idea/
*.iml
.externalNativeBuild/
.cxx/

# NOTE: gradlew/gradlew.bat is intentionally NOT ignored —
# the CI workflow generates them with 'gradle wrapper --gradle-version 8.11.1'.
# Local dev: run 'gradle wrapper' once after cloning.